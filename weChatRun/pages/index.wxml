

<view clas="mainView">
    <!--标题-->
    <view class="header">
        <view class="bigTitle" >跑步小助手</view>
        <view class="desc">健康生活每一天</view>
    </view>
    
    
    
    <view class="body">
        <view class="widget">
            <block wx:for="{{pageNames}}">
                <view class="widgets__item">
                <navigator url="{{item.id}}/{{item.id}}">{{item.name}}</navigator>    
                </view>
            </block>
        </view>
    </view>
</view>


